[advisories]
ignore = [
    # Transitive dependency of zenoh-transport 1.10.1. zenoh 1.10.1 is the
    # latest available release and still constrains lz4_flex to ^0.10.0.
    "RUSTSEC-2026-0041",
    # Transitive dependency of zenoh-transport 1.10.1 with no fixed rsa release.
    "RUSTSEC-2023-0071",
]
